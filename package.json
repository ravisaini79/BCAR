{
  "name": "bcar",
  "version": "1.0.0",
  "private": true,
  "description": "Business Correspondent Association Rajasthan portal",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "start": "concurrently -n API,WEB -c blue,magenta \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "start:frontend": "npm start --prefix frontend",
    "start:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix frontend && npm test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}